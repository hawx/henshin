doctype html
html lang='en'
  head
    meta charset='utf-8'
    link rel='stylesheet' href=style.url
    title = title

  body onload="window.scrollBy(0,54);"

    #top
      - if prev_post
        a.prev href=prev_post.url Prev

      a href=site.root Home

      - if next_post
        a.next href=next_post.url Next

    header
      h1 = title

    == yield

    div.meta
      span.date
        ' &mdash; #{date.strftime("%B %e, %Y")}

    script src='http://alexgorbatchev.com/pub/sh/current/scripts/shCore.js'
    script src='http://alexgorbatchev.com/pub/sh/current/scripts/shAutoloader.js'
    script src=script.url
