---
template: none
permalink: /feed.xml
---

doctype xml
rss version='2.0'
  channel
    title = site.title
    description = site.description
    link = site.url

    - for post in posts
      item
        title = post.title
        description = post.text
        published = post.date
        link = site.url + post.url
