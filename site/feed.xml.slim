---
template: none
permalink: /feed.xml
---

doctype xml
rss version='2.0' xmlns:atom="http://www.w3.org/2005/Atom"
  channel
    atom:link rel='self' type='application/rss+xml' href="#{File.join(site.url, permalink)}"
    title = site.title
    description = site.description
    link = site.url

    - for post in site.posts.take(10)
      item
        title = post.title
        description = post.text
        pubDate = post.date.strftime("%a, %d %b %Y %H:%M:%S %z")
        link = site.url + post.url.to_s
