- Make a logger/use an existing logger with different levels, hook up to --verbose
  and --quiet flags
- Only require engine files needed
- Allow user to set highlighting engine
- Make it faster
- Need to be able to pass options to rendering engines
